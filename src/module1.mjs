import n,{b,c} from './module.mjs';
//n is import by default with any name and b and c import same name as export from module.mjs
// if we use .js then it will show error for fetching by default file with diff name so we use .mjs

console.log(n);
console.log(b);
console.log(c);
